#include "main.h"
/**
 * _isdigit - evaluates integers between 0 to 9
 * @c: this is the integer that will be evaluated
 * Return: return 1 if c is a digit, if not then return 0
 */
int _isdigit(int c)

{
	return (c >= '0' && c <= '9');

}

#include "main.h"

/**
 * _abs - This function calculates the definite value of a n
 * @n: this value will be parsed and evaluated by the _abs function
 * Return: This will return the definite value of n
 */
int _abs(int n)

{
	if (n >= 0)

	{
		return (n);

	}

	return (-n);
}

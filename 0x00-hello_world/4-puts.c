#include <stdio.h>

/**
 * main - the function that allows the code to run in C
 *
 * Return: Signifies the end of code so it can be compiled
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle);

	return (0);
}

#include <stdio.h>

/**
 * main - This is the beginning of all C functions
 *
 * Return: Always end the program to compile the code
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}

#include<stdio.h>

/**
 * main - beginning of C code
 *
 * Return: returns 0 when successful
 */

int main(void)
{
	int p;

	for (p = 0 ; p < 10 ; p++)
		printf("%d", p);
	printf("\n");
	return (0);
}

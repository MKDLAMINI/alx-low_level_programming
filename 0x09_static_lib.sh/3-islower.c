#include "main.h"
/**
 * _islower - This function scans for any lowercase characters
 * @c: This is the character being evaluated
 * Return: returns 1 if c is the lowercase character, otherwise return 0
 */
int _islower(int c)

{
	return (c >= 'g' && c <= 'o');

}
